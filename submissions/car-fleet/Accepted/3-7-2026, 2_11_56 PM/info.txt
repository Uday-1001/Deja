{"id":1940610111,"question_id":883,"lang":"cpp","lang_name":"C++","time":"4 months, 2 weeks","timestamp":1772872916,"status":10,"status_display":"Accepted","runtime":"47 ms","url":"/submissions/detail/1940610111/","is_pending":"Not Pending","title":"Car Fleet","memory":"110.2 MB","code":"class Solution {\npublic:\n    using pi = pair<float,float>;\n\n    int carFleet(int target, vector<int>& position, vector<int>& speed) {\n        vector<pi>cars;\n        for(int i = 0 ; i<speed.size() ; i++)\n        {\n            auto pos = position[i];\n            auto s = speed[i];\n\n            cars.push_back({pos , s});\n        }\n\n        if(cars.size() == 1) return 1;\n        sort(cars.begin() , cars.end());\n\n        vector<float>time;\n        for(auto it : cars)\n        {\n            auto[pos , speed] = it;\n            time.push_back((target - pos) / speed);\n        }\n\n        vector<float>st;\n        for(auto it : time)\n        {\n            auto curr = it;\n            while(!st.empty() and curr >= st.back()) st.pop_back();\n            st.push_back(curr);\n        }\n    return st.size();\n    }\n};","compare_result":"1111111111111111111111111111111111111111111111111","title_slug":"car-fleet","has_notes":false,"flag_type":1,"frontend_id":1802}